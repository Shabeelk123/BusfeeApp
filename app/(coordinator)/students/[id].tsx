import StudentDetails from "../../../components/students/StudentDetails";

export default function CoordinatorStudentDetails() {
    return (
        <StudentDetails
            role="COORDINATOR"
            baseRoute="/(coordinator)"
        />
    );
}
